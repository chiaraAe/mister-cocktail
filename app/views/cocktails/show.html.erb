<h2><%= @cocktail.name %></h2> 
<div>
  <i class="fas fa-trash-alt"><%= link_to "Delete the drink", cocktail_path(@cocktail), method: :delete, data: { confirm: "Are you sure?" } %></i>
  <%= link_to "Delete the drink", cocktail_path(@cocktail), method: :delete, data: { confirm: "Are you sure?" } %>
</div>


<ul class="list-group">
  <% @cocktail.doses.each do |dose| %>
    <li class="list-group-item"><%= dose.description %> <%= dose.ingredient.name %> 
    <%= link_to "Delete", dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %> </li>
  <% end %>
</ul>



<%= link_to "Add Ingredience", new_cocktail_dose_path(@cocktail) %>
<%= link_to "Back", cocktails_path %>


